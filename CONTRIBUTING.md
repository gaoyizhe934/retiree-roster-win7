# 贡献指南

本项目面向 Windows 7 RTM 的离线退休人员名册管理与打印程序。贡献应保持变更范围清晰、可验证，并遵守本指南。

## 开始工作前

每个工作日开始时，先完成以下检查：

1. 列出当前问题、任务、解决方式、交付物和验收标准。
2. 明确必须完成项、可选项和不在当日范围内的事项，避免自行扩展需求。
3. 确认工作时间范围；临近截止时间优先验证、保存现场和交接。
4. 检查仓库、当前分支、远程地址和 Git 状态。发现已有改动时，先报告并确认其归属。
5. 获取远端最新状态。需要同步时使用 `git pull`，不使用 rebase 改写历史。

## 分支命名

不得在 `main` 分支直接推送。每项独立工作从最新 `main` 创建分支。

分支名格式为：

```text
类型/简短中文主题
```

允许的类型为 `feat`、`fix`、`docs`、`test`、`refactor` 或 `chore`。禁止使用 `codex/` 前缀。

示例：

```text
feat/实现表头识别
docs/补充贡献指南
fix/修复日期校验
```

## 提交规范

- 一个提交只处理一个可审查主题。不要把无关的格式化、重命名和功能修改混入同一提交。
- 提交信息使用中文，并采用下列格式：`类型: 中文变更摘要`。
- 类型与分支一致：`feat`、`fix`、`docs`、`test`、`refactor` 或 `chore`。
- 与 Git、PR 和评审相关的正文、评论和说明均使用中文。

示例：

```text
feat: 实现标准表头识别
docs: 补充贡献指南和项目说明
test: 增加日期边界测试
```

## 提交前审核

默认不执行 `git add`、提交或推送，直到变更通过人工审核并获得明确授权。提交前应提供：

1. 修改文件清单和逐文件修改原因。
2. Diff 摘要。
3. 已运行的检查、测试或构建结果。
4. 已知风险、阻塞项和未覆盖场景。
5. 建议的提交信息。

实质修改发生在审核之后时，应重新提供审核材料。`commit`、`push`、创建 PR 和合并 PR 分别需要明确授权。

## Pull Request 规范

- 推送分支前，先同步 `main` 并处理冲突；不要覆盖他人改动或改写历史。
- PR 标题使用 `类型: 中文变更摘要`。如需记录开发日和轨道，在 PR 正文首行填写 `阶段/轨道：D{阶段}{轨道}`，例如 `阶段/轨道：D1B`。
- 新的一天创建新的 PR，不修改其他人的 PR。
- 评审要求修改后，在原 PR 追加中文修改报告评论，不改写原始 PR 正文。
- PR 合并前应完成关联的验证，并由授权人员确认。

## 质量与数据边界

- 修改后按风险运行适当的构建、测试、导入校验或文档检查，并如实报告失败和限制。
- 真实人员数据、身份证号、电话、家庭地址、备份和未授权工作资料不得上传到公开仓库。
- 数据库、导入、筛选和打印模块必须使用 `include/retiree_roster/schema_types.hpp` 中的集中式类型契约，不应自行定义相同字段的私有 DTO。
- 记录技术债时，先更新技术债文档，再创建同编号的 GitHub Issue；编号格式为 `TD-年份-三位流水号`。

## 收尾与交接

结束当天工作时，汇总已完成项、未完成项、阻塞原因、当前 Git 状态和下次继续的起点。不要在未授权情况下删除资料、覆盖未知改动或继续扩展任务。
